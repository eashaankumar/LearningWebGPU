https://eliemichel.github.io/LearnWebGPU/

WebGPU Version: Dawn (Google)
GPU-Native NOT SUPPORTED (Yet)

Build:

`mkdir build`
`cmake . -B build`

Compile:

`cmake --build build`
OR
`compile.bat`

Run:

`.\build\Debug\App.exe`
OR
`run.bat`